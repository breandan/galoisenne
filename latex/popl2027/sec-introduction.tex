\documentclass[popl.tex]{subfiles}
\begin{document}

\section{Introduction}\label{sec:intro}

A missing delimiter or mistyped keyword can turn an otherwise meaningful program into an unrecognizable string. Typical parsers can detect invalidity, but cannot infer what the author intended to write. Suppose the parser instead responds by proposing a small set of valid alternatives. What should that set contain? We propose each suggestion should be, (1) syntactically valid, (2) similar to the original string, (3) highly natural. Producing a single repair with any one of these properties individually is relatively straightforward. Producing a small set of repairs satisfying all three properties -- without missing the author's intended repair -- is substantially harder.

This paper studies that problem for syntax defined by an arbitrary context-free grammar. Let \(G\) describe the language, let \(\err\sigma\notin\mathcal{L}(G)\) be an invalid token sequence, and let \(L(\err\sigma,d)\) denote the strings within \(d\) edits of \(\err\sigma\). The natural candidate space is \( \ell_\cap = \mathcal{L}(G)\cap\mathcal{L}\big(L(\err\sigma,d)\big)\). The grammar enforces validity, the edit language enforces locality, and a probabilistic model supplies naturalness. Assuming the intended repair requires fewer than $d$ edits, it will be contained in \(\ell_\cap\) and the problem becomes how to represent this finite language and quickly recover its members.

Classical error-correcting parsers return one repair, or the set minimizing a hand-designed cost~\cite{aho1972minimum,diekmann2018dont}. While appealing for their interpretability and well-understood algorithmic properties, these methods are unable to handle ambiguity and must rely on relatively brittle heuristics to select the repair. Learning-based systems~\cite{yasunaga2021break,sakkas2022seq2parse}, while statistically more robust, typically use some form of approximate inference and thus require expensive postprocessing or rejection sampling to ensure the generated results conform to the grammar. In either case, ranking is powerless if the candidate set does not contain the intended repair. We argue that for local syntax errors, completeness must be a first-class objective: a repair system must retrieve broadly before selecting the best repair(s).

Our approach makes the local search space explicit. We construct an acyclic Levenshtein automaton denoting every string within distance \(d\) of \(\err\sigma\), then intersect it with the grammar. Directly materializing the classical Bar--Hillel product grammar introduces many unreachable and non-generating productions. Instead, we exploit a correspondence between Bar--Hillel intersection and CFL reachability to quickly construct the grammar. Note that, while finite, \(\ell_\cap\) may be very large.

This representation turns repair into a constrained decoding problem, allowing any off-the-shelf autoregressive decoder to traverse \(\ell_\cap\) incrementally without rejection sampling. A lightweight WDFA-based decoder provides an initial ranking over up to millions of repair candidates, of which the top-512 are then scored with a pretrained transformer, reranked and finally truncated. Syntactic validity is enforced by construction, edit locality is exact, and transformer-based reranking is reserved for the highest-probability results, ensuring low latency and high naturalness.
\clearpage

\begin{figure}[H]
  \includegraphics[width=\textwidth]{flow/flow}\vspace{-0.1cm}
  \caption{Tidyparse represents the bounded edit neighborhood as an automaton, intersects it with the language grammar, and compiles the finite intersection into a generalized regular expression. A probabilistic decoder enumerates likely members, which a learned model reranks for presentation.}\label{fig:arch_simp}\vspace{-0.2cm}
\end{figure}

This paper makes three contributions. First, it formulates bounded syntax repair as exact language intersection and gives a practical construction for arbitrary context-free grammars. Second, it gives a parallel matrix-closure procedure for intersection nonemptiness of a CFG and an acyclic NFA, proves that the closure phase cannot require more than a linear number of squaring rounds, and develops derivative-based decoding algorithms. Third, it implements these ideas in Tidyparse on multicore CPUs and GPUs. On naturally occurring Python errors of up to three lexical edits and eighty tokens, Tidyparse operates within subsecond latency and achieves up to a fivefold improvement in top-1 repair precision over prior systems under comparable resource limits.
\ifSubfilesClassLoaded{\par\clearpage\bibliography{../bib/acmart}}{}
\end{document}
