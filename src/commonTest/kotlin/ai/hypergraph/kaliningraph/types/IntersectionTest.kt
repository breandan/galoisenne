package ai.hypergraph.kaliningraph.types

import kotlin.test.*

/*
./gradlew jvmTest --tests "ai.hypergraph.kaliningraph.types.IntersectionTest"
*/
class IntersectionTest {
  @Test
  fun testIntersection() {
    val bar = Both()

//    bar.asIntersection3(Interface1::class, Interface2::class, Interface3::class)?.let { baz ->
//      baz // Check the type here in Intellij with `ctrl+shift+p`
//      println(baz.i1)
//      println(baz.i2)
//      println(baz.i3)
//    }
  }
}